        // auto maxAtNode = [](const vector<int>& v1, const vector<int>& v2) {
        //     int max1 = *max_element(v1.begin(), v1.end());
        //     int max2 = *max_element(v2.begin(), v2.end());
        //     return max1 < max2;
        // };

        // sort(linkedDistances.begin(), linkedDistances.end(), maxAtNode);
